<%= form_for([@post, @post.comments.build]) do |f| %>
    <%= f.label :body %>
    <%= f.text_field :body %>
    <%= f.label :rating %>
    <%= f.text_field :rating %>
    <%= f.submit "Create"%>
<% end %>
